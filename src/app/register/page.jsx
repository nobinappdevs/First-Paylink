import RegistrationPage from '@/components/authentication/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegistrationPage />
        </div>
    );
};

export default page;