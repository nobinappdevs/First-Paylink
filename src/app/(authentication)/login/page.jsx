import LoginPage from '@/components/authentication/Login';
import React from 'react';

const page = () => {
    return (
        <div>
          <LoginPage />  
        </div>
    );
};

export default page;