import TicketDashboard from '@/components/dashboard/helpCenter/TicketDashboard';
import React from 'react';

const page = () => {
    return (
        <div>
          this is helpCenter page  
          <TicketDashboard />
        </div>
    );
};

export default page;